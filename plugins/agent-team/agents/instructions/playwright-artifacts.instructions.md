---
applyTo: "**/coordinator.agent.md,**/designer.agent.md,**/developer.agent.md,**/platform-engineer.agent.md,**/reviewer.agent.md,**/tester.agent.md"
---

# Playwright Artifacts

Save Playwright-generated artifacts (screenshots, snapshots, videos) to `.playwright-cli/` (gitignored). Clean up after checks by default — keep only when the user explicitly asks or as failure evidence.
